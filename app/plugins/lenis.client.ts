// If you want add Lenis - just install dependency 'npm i @studio-freight/lenis',
// uncomment this file, move to _general.scss and uncomment 'Lenis recommended styles'

// import Lenis from "@studio-freight/lenis"

export default defineNuxtPlugin(() => {
  // const { $gsap: g, $ScrollTrigger: ScrollTrigger } = useNuxtApp()
  // const lenis = useState("lenis", () => {
  //   return new Lenis({
  //     duration: 1.2,
  //     orientation: "vertical",
  //     smoothWheel: true,
  //   })
  // })
  // lenis.value.on("scroll", ScrollTrigger.update)
  // g.ticker.add((time) => {
  //   lenis.value.raf(time * 1000)
  // })
  // g.ticker.lagSmoothing(0)
})
